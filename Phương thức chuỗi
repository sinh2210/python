Capitalize() Chuyển ký tự đầu tiên thành chữ hoa
casefold() Chuyển chuỗi thành chữ thường
center() Trả về một chuỗi ở giữa
count() Trả về số lần một giá trị được chỉ định xuất hiện trong một chuỗi
Encode() Trả về phiên bản đã mã hóa của chuỗi
endswith() Trả về true nếu chuỗi kết thúc bằng giá trị đã chỉ định
Expandtabs() Đặt kích thước tab của chuỗi
find() Tìm kiếm chuỗi cho một giá trị được chỉ định và trả về vị trí nơi nó được tìm thấy
format() Định dạng các giá trị được chỉ định trong một chuỗi
format_map() Định dạng các giá trị được chỉ định trong một chuỗi
index() Tìm kiếm chuỗi cho một giá trị được chỉ định và trả về vị trí nơi nó được tìm thấy
isalnum() Trả về True nếu tất cả các ký tự trong chuỗi đều là chữ và số
isalpha() Trả về True nếu tất cả các ký tự trong chuỗi đều nằm trong bảng chữ cái
isascii() Trả về True nếu tất cả các ký tự trong chuỗi đều là ký tự ascii
isdecimal() Trả về True nếu tất cả ký tự trong chuỗi là số thập phân
isdigit() Trả về True nếu tất cả ký tự trong chuỗi đều là chữ số
isidentifier() Trả về True nếu chuỗi là định danh
islow() Trả về True nếu tất cả các ký tự trong chuỗi đều là chữ thường
isnumeric() Trả về True nếu tất cả các ký tự trong chuỗi đều là số
isprintable() Trả về True nếu tất cả các ký tự trong chuỗi đều có thể in được
isspace() Trả về True nếu tất cả ký tự trong chuỗi đều là khoảng trắng
istitle() Trả về True nếu chuỗi tuân theo quy tắc của tiêu đề
isupper() Trả về True nếu tất cả các ký tự trong chuỗi đều là chữ hoa
join() Nối các phần tử của một iterable vào cuối chuỗi
ljust() Trả về phiên bản canh lề trái của chuỗi
low() Chuyển đổi một chuỗi thành chữ thường
lstrip() Trả về phiên bản cắt bên trái của chuỗi
maketrans() Trả về bảng dịch được sử dụng trong bản dịch
partition() Trả về một bộ trong đó chuỗi được chia thành ba phần
replace() Trả về một chuỗi trong đó một giá trị đã chỉ định được thay thế bằng một giá trị đã chỉ định
rfind() Tìm kiếm chuỗi cho một giá trị được chỉ định và trả về vị trí cuối cùng nơi nó được tìm thấy
rindex() Tìm kiếm chuỗi cho một giá trị được chỉ định và trả về vị trí cuối cùng nơi nó được tìm thấy
rjust() Trả về phiên bản canh lề phải của chuỗi
rpartition() Trả về một bộ trong đó chuỗi được chia thành ba phần
rsplit() Tách chuỗi tại dấu phân cách đã chỉ định và trả về một danh sách
rstrip() Trả về phiên bản cắt bên phải của chuỗi
Split() Tách chuỗi tại dấu phân cách đã chỉ định và trả về một danh sách
Splitlines() Tách chuỗi tại chỗ ngắt dòng và trả về một danh sách
startswith() Trả về true nếu chuỗi bắt đầu bằng giá trị đã chỉ định
Strip() Trả về phiên bản rút gọn của chuỗi
swapcase() Hoán đổi chữ hoa, chữ thường thành chữ hoa và ngược lại
title() Chuyển ký tự đầu tiên của mỗi từ thành chữ hoa
translate() Trả về chuỗi đã dịch
Upper() Chuyển một chuỗi thành chữ hoa
zfill() Điền vào chuỗi với số lượng giá trị 0 được chỉ định ở đầu
